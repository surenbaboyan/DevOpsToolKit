### Create New User
 - Run the play_create_user.yml Ansible playbook file with the command: 
   - **ansible-playbook play_create_user.yml**
 - Enter the desired **username** when prompted.
 - Enter the **SSH public** key when prompted.